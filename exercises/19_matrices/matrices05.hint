`0..2` is rows 0 and 1: the top. The last two of three are rows 1 and 2,
which is `1..3`, or `1..` for "from 1 to the end". The same for the columns:
`a <- slice (1..) (1..)`. The view on the last line already says it.
