`take 2` stops after two values; the first three even squares need `take 3`.
Without any `take`, `collect$` would ask an endless range for every value and
never finish: laziness is only safe when something stops asking.
