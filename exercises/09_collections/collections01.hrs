// collections01
//
// A `Vec` grows with `push`. Fill the vector so the assertion holds.
//
// Run `hrs run -- hint collections01` for a hint.

// I AM NOT DONE

fn main$:
    let mut v: Vec<i32> = Vec.new$
    assert_eq! v ([10, 20, 30])
    println! "{v:?}"
