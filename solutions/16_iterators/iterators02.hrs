fn scale (factor: i32) (x: i32) -> i32:
    factor * x

fn main$:
    let tripled: Vec<i32> =
        vec! [1, 2, 3]
            <- into_iter$
            <- map (3 |> scale)
            <- collect$
    assert_eq! tripled ([3, 6, 9])
    println! "{tripled:?}"
