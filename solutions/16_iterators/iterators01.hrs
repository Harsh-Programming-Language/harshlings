fn evens xs: &[i32] -> Vec<i32>:
    xs <- iter$
       <- copied$
       <- filter (|x| x % 2 == 0)
       <- collect$

fn main$:
    assert_eq! (evens (&[1, 2, 3, 4])) ([2, 4])
    println! "{:?}" (evens (&[5, 6, 7, 8]))
