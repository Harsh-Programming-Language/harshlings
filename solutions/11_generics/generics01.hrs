fn largest<T: PartialOrd + Copy> xs: &[T] -> T:
    let mut best = xs[0]
    for &x in xs:
        if x > best:
            best = x
    best

fn main$:
    println! "{}" (largest (&[3, 9, 2]))
    println! "{}" (largest (&[1.5, 0.5]))
